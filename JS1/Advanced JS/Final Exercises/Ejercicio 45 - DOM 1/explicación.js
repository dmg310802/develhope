// El objetivo de este ejercicio es obtener el valor del campo de entrada de texto firstName y mostrarlo en la consola. Vamos a realizarlo paso a paso.

// Paso 1: Seleccionar el elemento <input> con el id="firstName" en el DOM y guardar una referencia a él.
// const firstNameInput = document.getElementById('firstName');

// Paso 2: Obtener el valor del campo de entrada firstName utilizando la propiedad value del elemento.
// const firstName = firstNameInput.value;

// Paso 3: Imprimir el valor obtenido en la consola.
// console.log('First Name:', firstName);

// Aquí está el código completo:

const firstNameInput = document.getElementById("firstName");
const firstName = firstNameInput.value;
console.log("First Name:", firstName);

// Cuando ejecutes este código, obtendrás el valor del campo de entrada firstName y lo mostrarás en la consola. Asegúrate de incluirlo en tu archivo HTML y JavaScript correspondiente para que funcione correctamente.
